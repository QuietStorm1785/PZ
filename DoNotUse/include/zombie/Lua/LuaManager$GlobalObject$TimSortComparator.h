#pragma once
#include <string>
#include <vector>
#include <memory>
#include <unordered_map>
#include <unordered_set>
#include <cstdint>
#include <algorithm>

namespace zombie {
namespace Lua {


class LuaManager {
public:
    void* comp;

   private LuaManager$GlobalObject$TimSortComparator() {
   }

    int compare(void* var1, void* var2) {
      if (Objects == var1, var2)) {
    return 0;
      } else {
    bool var3 = LuaManager.thread.pcallBoolean(this.comp, var1, var2);
    return var3 = = bool.TRUE ? -1 : 1;
      }
   }
}
} // namespace Lua
} // namespace zombie
